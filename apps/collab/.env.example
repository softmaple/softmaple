DATABASE_URL=
DIRECT_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Optional local-only bypass (also requires NODE_ENV=development).
# Tokens: "dev:<userId>" or legacy "dev:<userId>:<ROLE>" — role always from workspaceMember.
COLLAB_DEV_AUTH_BYPASS=false
PORT=4001
