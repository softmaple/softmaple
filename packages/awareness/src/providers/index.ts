/**
 * Export awareness context providers
 */

// Placeholder for provider exports
// export { PresenceProvider } from "./PresenceProvider";
// export { PresenceContext } from "./PresenceContext";

// Temporary placeholder export
export const AwarenessProvider = ({
  children,
}: {
  children: React.ReactNode;
}) => {
  return children;
};
